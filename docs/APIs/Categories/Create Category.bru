meta {
  name: Create Category
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/categories
  body: json
  auth: inherit
}

body:json {
  {
    "name": "New Category Name"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
