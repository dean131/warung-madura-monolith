meta {
  name: Logout
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/logout
  body: json
  auth: inherit
}

body:json {
  {
    "refreshToken": "{{refreshToken}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
